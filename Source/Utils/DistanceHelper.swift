//
//  DistanceHelper.swift
//  PaymentezSDK
//
//  Created by Fennoma on 22/04/2021.
//

import Foundation
//import PromiseKit

class DistanceHelper {
    
    /*static func distanceBetween(location1: CLLocation, location2: CLLocation) -> String {
        return stringForMeters(meters: location1.distance(from: location2))
    }
    
    static func stringForMeters(meters: Double) -> String {
        var result = ""
        if (meters < 1000) {
            result = String(format: "%.1f mt", meters)
        } else {
            result = String(format: "%.1f Km", meters/Double(1000))
        }
        if result != "" {
            result = result.replacingOccurrences(of: ".", with: ",")
        }
        return result
    }*/
    
}
